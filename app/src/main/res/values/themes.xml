<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PaperTrader" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.PaperTrader" parent="Base.Theme.PaperTrader" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/login_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/pt_logo</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.PaperTrader</item>
    </style>

</resources>